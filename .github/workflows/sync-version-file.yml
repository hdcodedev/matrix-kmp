# Workflow: keep the checked-in .version file in sync with Axion.
#
# This runs on main branch pushes.
name: Sync Version File

on:
  push:
    branches:
      - main

permissions:
  contents: write
  pull-requests: write

concurrency:
  group: sync-version-file-${{ github.ref }}
  cancel-in-progress: true

jobs:
  sync-version:
    name: Sync .version from Axion currentVersion
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Set up JDK 17
        uses: actions/setup-java@v5.6.0
        with:
          java-version: 17
          distribution: zulu
          cache: gradle

      - name: Resolve version and update .version
        id: version
        shell: bash
        run: |
          set -euo pipefail
          matrix_version="$(./gradlew -q currentVersion | awk 'NF {v=$NF} END {gsub(/\r/, "", v); print v}')"
          if [[ -z "${matrix_version:-}" ]]; then
            echo "Failed to resolve version from Axion currentVersion task." >&2
            exit 1
          fi
          printf '%s\n' "${matrix_version}" > .version
          echo "matrix_version=${matrix_version}" >> "$GITHUB_OUTPUT"

      - name: Publish resolved version in run summary
        shell: bash
        run: |
          set -euo pipefail
          echo "### Synced Version" >> "$GITHUB_STEP_SUMMARY"
          echo "- Version: \`${{ steps.version.outputs.matrix_version }}\`" >> "$GITHUB_STEP_SUMMARY"
          echo "- File: \`.version\`" >> "$GITHUB_STEP_SUMMARY"

      - name: Validate workflow token
        shell: bash
        env:
          MATRIX_WORKFLOW_TOKEN: ${{ secrets.MATRIX_WORKFLOW_TOKEN }}
        run: |
          set -euo pipefail
          if [[ -z "${MATRIX_WORKFLOW_TOKEN:-}" ]]; then
            echo "Missing required secret: MATRIX_WORKFLOW_TOKEN." >&2
            exit 1
          fi

      - name: Create PR when .version changed
        uses: peter-evans/create-pull-request@v8
        with:
          token: ${{ secrets.MATRIX_WORKFLOW_TOKEN }}
          add-paths: .version
          branch: chore/sync-version-file
          delete-branch: true
          commit-message: "chore: sync .version"
          title: "chore: sync .version"
          body: |
            Automated update of `.version` from `./gradlew currentVersion`.

            Resolved version: `${{ steps.version.outputs.matrix_version }}`
          labels: |
            automation
            chore
